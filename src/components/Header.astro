<header class="site-header">
  <div class="header-inner wide-column">
    <a class="site-name" href="/" aria-label="На главную">Константин Вершигора</a>
    <nav aria-label="Основная навигация">
      <a href="/#projects-title">Проекты</a>
      <a href="mailto:versh.konstantin@gmail.com">Связаться</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--color-line);
  }

  .header-inner {
    display: flex;
    min-height: 4.5rem;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
  }

  .site-name {
    color: var(--color-text);
    font-weight: 650;
    letter-spacing: -0.02em;
    text-decoration: none;
  }

  nav {
    display: flex;
    gap: clamp(1rem, 4vw, 2rem);
  }

  nav a {
    color: var(--color-muted);
    font-size: 0.95rem;
    text-decoration: none;
  }

  nav a:hover,
  nav a:focus-visible {
    color: var(--color-text);
  }

  @media (max-width: 480px) {
    .header-inner {
      min-height: 4rem;
    }

    nav a:first-child {
      display: none;
    }
  }
</style>
