<nav id="contact-dropdown" class="contact-dropdown" aria-label="Контактные ссылки">
  <a
    class="contact-dropdown__link contact-dropdown__link--email"
    href="mailto:versh.konstantin@gmail.com"
    target="_blank"
    rel="noopener noreferrer"
  >
    <img src="/icons/email.svg" width="32" height="32" alt="" aria-hidden="true" />
    <span>Написать на почту</span>
  </a>
  <a
    class="contact-dropdown__link contact-dropdown__link--telegram"
    href="https://t.me/k0stip"
    target="_blank"
    rel="noopener noreferrer"
  >
    <img src="/icons/telegram.svg" width="32" height="32" alt="" aria-hidden="true" />
    <span>Написать в Telegram</span>
  </a>
</nav>

<style>
  .contact-dropdown {
    position: absolute;
    z-index: 20;
    top: 100%;
    right: 0;
    display: flex;
    width: max-content;
    max-width: calc(100vw - 20px);
    flex-direction: column;
    gap: 20px;
    padding: 20px;
    border-radius: 12px;
    visibility: hidden;
    transform: translateY(-4px) scale(0.98);
    transform-origin: top right;
    background: var(--color-gray-button);
    opacity: 0;
    pointer-events: none;
    transition:
      opacity 140ms ease-in,
      transform 140ms ease-in,
      visibility 0s linear 140ms;
  }

  :global(.contact-area.is-open) .contact-dropdown {
    visibility: visible;
    transform: translateY(0) scale(1);
    opacity: 1;
    pointer-events: auto;
    transition:
      opacity 180ms ease-out,
      transform 180ms ease-out,
      visibility 0s;
  }

  .contact-dropdown__link {
    display: flex;
    align-items: center;
    gap: 12px;
    color: var(--color-black-text);
    font-size: 16px;
    font-weight: 500;
    line-height: normal;
    text-decoration: none;
    white-space: nowrap;
  }

  .contact-dropdown__link img {
    width: 32px;
    height: 32px;
    flex: 0 0 32px;
  }

  .contact-dropdown__link span {
    transition: color 180ms ease-out;
  }

  .contact-dropdown__link--email:hover span,
  .contact-dropdown__link--email:focus-visible span {
    background: var(--gradient-default);
    background-clip: text;
    color: transparent;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .contact-dropdown__link--telegram:hover span,
  .contact-dropdown__link--telegram:focus-visible span {
    color: var(--color-blue-telegram);
  }

  @media (min-width: 840px) {
    :global(.contact-area:not(.is-force-closed):hover) .contact-dropdown,
    :global(.contact-area:not(.is-force-closed):focus-within) .contact-dropdown {
      visibility: visible;
      transform: translateY(0) scale(1);
      opacity: 1;
      pointer-events: auto;
      transition:
        opacity 180ms ease-out,
        transform 180ms ease-out,
        visibility 0s;
    }
  }

  @media (max-width: 839px) {
    .contact-dropdown {
      border-radius: 8px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .contact-dropdown,
    :global(.contact-area.is-open) .contact-dropdown,
    :global(.contact-area:not(.is-force-closed):hover) .contact-dropdown,
    :global(.contact-area:not(.is-force-closed):focus-within) .contact-dropdown {
      transform: none;
      transition-duration: 1ms;
    }
  }
</style>
